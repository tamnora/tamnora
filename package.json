{
  "name": "vite-tamnora",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"npx tailwindcss -i ./input.css -o ./src/css/style.css --watch\"",
    "build": "rimraf ./dist && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "8.2.0",
    "rimraf": "5.0.1",
    "tailwindcss": "3.3.3",
    "vite": "4.4.5"
  }
}
